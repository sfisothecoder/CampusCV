"use client";

import React from "react";

const BetaBadge = () => (
  <span className="inline-flex items-center rounded-full border border-primary-200 bg-primary-50 px-2.5 py-1 text-[0.625rem] font-semibold uppercase tracking-[0.2em] text-primary-700">
    beta
  </span>
);

export default BetaBadge;
